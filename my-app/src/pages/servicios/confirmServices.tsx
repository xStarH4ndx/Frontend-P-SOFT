import React from "react";
import { Container } from "@mui/material";
import { useNavigate } from "react-router";


export const ConfirmServicePage:React.FC<{}> = () => {
    const navigate = useNavigate();
    return (
        <Container>

        </Container>
    )
}